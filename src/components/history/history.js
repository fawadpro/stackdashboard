import React from 'react'
import { createBrowserHistory } from 'history';

export default function createBrowserHistory() {
    //pass configuration object if we need

    console.log("I am function")
}
